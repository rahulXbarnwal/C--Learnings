using System;

/*
 1. Abstract classes can have implementations for some of its members, but interfaces can't have implementation for any of its members
 2. Interfaces can't have fields but abstract classes can have fields
 3. An interface can inherit from interface only and not from an abstract class, but abstract class can inherit from another abs class or interface
 4. A class can inherit from multiple interfaces at the same time, but a class can't inherit from multiple classes at the same time
 5. Abstract class members can have access modifiers whereas interface members can't have access modifiers
 */

namespace IntroToCsharp
{
    internal class AbstractClassesVsInterfaces
    {
    }
}
